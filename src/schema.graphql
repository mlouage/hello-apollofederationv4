type User {
  id: ID!
  name: String!
  email: String!
  favorites: [Product!]
}

type Product {
  id: ID!
  name: String!
  description: String!
  image: String!
  price: Price!
}

type Price {
  id: ID!
  price: Float!
  currency: String!
}

type Query {
  users: [User]
  user(id: ID!): User
  products: [Product]
  prices: [Price]
}
